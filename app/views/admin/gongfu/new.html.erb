<div id="middle" xmlns="http://www.w3.org/1999/html">
  <%= form_tag("/admin/gongfu/", :method => 'post', :multipart => true) do %>
      <table class="tabinfo">
        <tr>
          <th style="width: 20%">名称：</th>
          <td>
            <label for="gf_type"> </label>
            <select id="gf_type" name="gongfu[gf_type]">
              <% @gongfu_config.keys.each() do |e| %>
                  <option value=<%= "\"#{@names_config[@gongfu_config[e]["name"]]}\"".html_safe %>>
                    <%= @names_config[@gongfu_config[e]["name"]] %></option>
              <% end %>
            </select>
          </td>
        </tr>
        <tr>
          <th style="width: 20%">等级：</th>
          <td>
            <%= text_field(:gongfu, :level ) %>
                <% if @gongfu.errors['level'] && @gongfu.errors['level'].length > 0 %>
                    <span style="color: red;">功夫等级输入不合法,应为数字，不超过100。</span>
                <% end %>
          </td>
        </tr>
        <tr>
          <th style="width: 20%">经验：</th>
          <td>
            <%= text_field(:gongfu, :experience) %>
            <% if @gongfu.errors['experience'] && @gongfu.errors['experience'].length > 0 %>
                <span style="color: red;">经验输入不合法,应为数字。</span>
            <% end %>
          </td>
        </tr>
        <tr>
          <th style="width: 20%">成长强度：</th>
          <td>
            <%= text_field(:gongfu, :grow_strength ) %>
            <% if @gongfu.errors['grow_strength'] && @gongfu.errors['grow_strength'].length > 0 %>
                <span style="color: red;">成长强度输入为浮点数或整数。</span>
            <% end %>
          </td>
        </tr>
      </table>
      <input type="submit" value="添加功夫" class="btnsubmit"/>
  <% end %>
</div>
